name: Aplicar revisión Tema 18

on:
  push:
    branches:
      - rebuild/puebla-tema-18

permissions:
  contents: write

jobs:
  apply:
    if: github.event.head_commit.message != 'Reconstruir Tema 18 para revisión'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: rebuild/puebla-tema-18
          fetch-depth: 0

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Aplicar reconstrucción
        run: python scripts/apply_t18_review.py

      - name: Validar contenido
        run: npm test

      - name: Publicar cambios en la rama
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Reconstruir Tema 18 para revisión"
          git push origin HEAD:rebuild/puebla-tema-18
