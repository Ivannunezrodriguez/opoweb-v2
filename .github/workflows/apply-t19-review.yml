name: Aplicar revisión Tema 19

on:
  push:
    branches:
      - rebuild/puebla-tema-19

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rebuild/puebla-tema-19
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python scripts/apply_t19_review.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add content/la-puebla/tema-19 data/programa.json README.md tests/validate.mjs
          git commit -m "Reconstruir La Puebla · Tema 19 para revisión" || exit 0
          git push
